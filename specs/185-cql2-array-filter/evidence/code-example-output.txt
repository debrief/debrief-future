CQL2 array_filter Evaluator - Code Example Output
===================================================

Example 1: Compound filter - "British submarines"
--------------------------------------------------
Filter: array_filter(platforms, p -> p.nationality = 'GB' AND p.domain = 'subsurface')

Item A (mixed: GB surface + DE subsurface):
  platforms: [{id: "HMS_ARGYLL", nationality: "GB", domain: "surface"},
              {id: "U31", nationality: "DE", domain: "subsurface"}]
  Result: NO MATCH (no single platform satisfies both conditions)

Item B (GB subsurface):
  platforms: [{id: "HMS_ASTUTE", nationality: "GB", domain: "subsurface"}]
  Result: MATCH

Item C (two GB, surface + subsurface):
  platforms: [{id: "HMS_ARGYLL", nationality: "GB", domain: "surface"},
              {id: "HMS_ASTUTE", nationality: "GB", domain: "subsurface"}]
  Result: MATCH (second platform satisfies both)


Example 2: Taxonomy expansion - "British frigates"
---------------------------------------------------
Filter: array_filter(platforms, p -> p.nationality = 'GB' AND p.vessel_class = 'frigate')

Item (vessel_class = "surface/warship/frigate/type23"):
  Result: MATCH (taxonomy expansion: frigate -> type23, type26, fremm, ...)


Example 3: CQL2 JSON serialization
-----------------------------------
Input FilterExpression:
  arrayFilters: [{
    array: "platforms",
    predicate: { kind: "and", children: [
      { kind: "comparison", field: "nationality", value: "GB" },
      { kind: "comparison", field: "domain", value: "subsurface" }
    ]}
  }]

Output CQL2 JSON:
  {
    "op": "array_filter",
    "args": [
      { "property": "debrief:platforms" },
      {
        "op": "and",
        "args": [
          { "op": "=", "args": [{ "property": "nationality" }, "GB"] },
          { "op": "=", "args": [{ "property": "domain" }, "subsurface"] }
        ]
      }
    ]
  }


Example 4: Negation - "Exclude British submarines"
---------------------------------------------------
Filter: NOT array_filter(platforms, p -> p.nationality = 'GB' AND p.domain = 'subsurface')

Item with GB submarine: NO MATCH (excluded by negation)
Item with GB surface only: MATCH (no GB submarine platform)
Item with empty platforms: MATCH (no platforms to match against)


Example 5: Round-trip fidelity
------------------------------
1. Create FilterExpression with arrayFilters
2. Serialize to CQL2 JSON via engine.toCql2Json()
3. Deserialize back via cql2JsonToArrayFilters()
4. Evaluate both expressions against test items
Result: Identical match/no-match results before and after round-trip
