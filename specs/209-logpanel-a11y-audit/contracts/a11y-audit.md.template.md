---
feature: "176-log-panel-ux"
audit_feature: "209-logpanel-a11y-audit"
captured_at: "YYYY-MM-DDTHH:MM:SSZ"
git_sha: "abcdef1"
axe_version: "X.Y.Z"
storybook_version: "X.Y.Z"
axe_tags: ["wcag2a", "wcag2aa", "wcag21aa"]
axe_include_selector: "#storybook-root"
total_violations: 0
fix_now_remaining: 0
---

# LogPanel Accessibility Audit

> Generated by `shared/components/e2e/LogPanel-a11y-axe.spec.ts` +
> `shared/components/scripts/a11y-audit-report.ts`. Do **not** hand-edit except for
> the `Classification` and `Rationale` columns of findings; all other fields are
> regenerated on re-run. See `specs/209-logpanel-a11y-audit/quickstart.md` to re-run.

## Scope

- **Package**: `@debrief/components`
- **Story selector**: every Storybook entry whose `importPath` starts with `./src/LogPanel/`
  (covers `LogPanel.stories.tsx` + `ParameterEditor.stories.tsx` + any future LogPanel sub-component stories).
- **Themes**: `light`, `dark`, `vscode` — driven via `globals=theme:<name>` query parameter.
- **Scanner**: `@axe-core/playwright ^4.8.5` with `AxeBuilder.include('#storybook-root')`.
- **WCAG tags**: `wcag2a`, `wcag2aa`, `wcag21aa`.

## Coverage Matrix

Rows = stories, columns = themes. `✅` = pass (0 violations), `❌` = at least one violation, `⚠` = error (page failed to render or axe threw).

| Story | light | dark | vscode |
|-------|:-----:|:----:|:------:|
| `logpanel--timeline-default` | ✅ | ✅ | ✅ |
| `logpanel--empty-no-plot` | ✅ | ✅ | ✅ |
| `logpanel--entry-selected` | ✅ | ✅ | ✅ |
| … | … | … | … |

*(Filled by the post-processor from every CoverageEntry in the JSON dump.)*

## Findings

*(One subsection per unique `rule_id` observed across the matrix. Empty if no violations.)*

### `<rule-id>` — `<short description>`

- **Severity**: `serious` *(one of: minor, moderate, serious, critical)*
- **WCAG**: `wcag2aa`, `wcag143`
- **Help**: `<axe.help sentence>` — [docs](https://dequeuniversity.com/rules/axe/4.8/...)
- **Representative selector**: `.log-entry__header > time`
- **Selector varies across pairs**: `false`
- **Affected pairs**:
  - `logpanel--timeline-default` / `dark`
  - `logpanel--entry-selected` / `dark`
- **Classification**: `fix-now` *(one of: `fix-now`, `accepted`, `deferred`)*
- **Rationale**: *(free-form. Required when classification ≠ `fix-now`.)*
- **Backlog ref**: *(required when classification = `deferred`; must be a `#NNN` row in `BACKLOG.md`.)*

## Resolved Previously

*(Rules that appeared in an earlier run but no longer appear in the current run.
Retained to preserve the historical classification decision. Empty on first run.)*

| Rule | Last seen at SHA | Final classification | Rationale |
|------|------------------|----------------------|-----------|
| *(none)* | — | — | — |

## How to Reproduce

```sh
pnpm --filter @debrief/components a11y:audit
```

Full walkthrough: `specs/209-logpanel-a11y-audit/quickstart.md`.
