# Implementation Plan: Epic Support for Speckit Workflow

**Branch**: `023-epic-workflow-support` | **Date**: 2026-01-23 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `/specs/023-epic-workflow-support/spec.md`

## Summary

Add epic support to the speckit workflow by creating a new Epics section in BACKLOG.md and a `/epic` skill that uses Opus model to break down large features into deliverable backlog items with `[Ex]` traceability nomenclature.

## Technical Context

**Language/Version**: Markdown (skill definition), Bash (gh CLI integration)
**Primary Dependencies**: Claude Code Skill system, GitHub CLI (`gh`), Opus model
**Storage**: BACKLOG.md (epics and items tables), GitHub issues
**Testing**: Manual verification via skill execution, acceptance criteria checklist
**Target Platform**: Claude Code CLI (macOS/Linux)
**Project Type**: Single (workflow tooling)
**Performance Goals**: N/A (human-speed workflow)
**Constraints**: Must work with existing BACKLOG.md structure, graceful degradation when offline
**Scale/Scope**: 3-10 items per epic breakdown

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

| Article | Requirement | Status | Notes |
|---------|-------------|--------|-------|
| I.1 Offline by default | Core functionality without network | ⚠️ PARTIAL | `gh` needs network; fallback to local-only mode |
| I.3 No silent failures | Explicit success/failure | ✅ PASS | Skill reports all outcomes |
| VI.2 Services require tests | Unit tests for service code | ⚠️ N/A | No service code; skill is prompt-based |
| VII.1 Tests before implementation | Define expected behaviour first | ✅ PASS | Acceptance criteria in spec |
| VIII.1 Specs before code | Written specification | ✅ PASS | spec.md complete |
| XIII.1 Atomic commits | One logical change per commit | ✅ PASS | Will follow |

**Gate Decision**: ✅ PROCEED - Offline constraint addressed via fallback mode (local-only without GitHub issues)

## Project Structure

### Documentation (this feature)

```text
specs/023-epic-workflow-support/
├── spec.md              # Feature specification
├── plan.md              # This file
├── research.md          # Phase 0 output
├── data-model.md        # Phase 1 output (Epics table schema)
├── quickstart.md        # Phase 1 output (usage guide)
└── tasks.md             # Phase 2 output
```

### Source Code (repository root)

```text
.specify/skills/
└── epic.md              # New skill definition

BACKLOG.md               # Modified to add Epics section
```

**Structure Decision**: Single skill file + BACKLOG.md modification. No traditional source code - this is workflow tooling.

## Media Components

None - backend/infrastructure feature

*This feature adds workflow tooling without visual components. No Storybook stories applicable.*

## Complexity Tracking

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| I.1 Offline (partial) | GitHub issue creation requires network | Local-only fallback insufficient for full workflow; GitHub issues provide traceability and collaboration |

## Implementation Phases

### Phase 0: Research

- Existing skill file patterns (study other skills in `.specify/skills/`)
- BACKLOG.md table parsing patterns
- GitHub CLI issue creation best practices
- Opus model prompting for BA/Architect analysis

### Phase 1: Design

1. **Epics table schema** (data-model.md)
2. **Skill file structure** (following existing patterns)
3. **Usage quickstart** (quickstart.md)

### Phase 2: Implementation Tasks

See `tasks.md` (generated by `/speckit.tasks`)
