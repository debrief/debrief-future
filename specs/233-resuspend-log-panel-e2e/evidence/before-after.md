---
feature: 233-resuspend-log-panel-e2e
captured_at: 2026-04-29
git_sha: 9bbdd0e
---

# Before / After — Pre-PR vs Post-PR State

| Aspect | Before (main, post-#534) | After (this branch merged) | Evidence | FR |
|--------|--------------------------|----------------------------|----------|----|
| `tests/e2e/test-log-panel.spec.ts` describe wrapper | `test.describe.fixme(...)` (suite muted, 0/5 in CI) | `test.describe(...)` (suite active, 5/5 passing) | `tests/e2e/test-log-panel.spec.ts:11` | FR-001 |
| `tests/e2e/test-log-panel.spec.ts` mute comment block | 8-line `// #233 — Re-suspended pending #142 ...` | Removed | `tests/e2e/test-log-panel.spec.ts:1-12` (post-PR) | FR-002 |
| Local Playwright run (single shot) | 0/5 (whole suite skipped) | 5 passed, 0 failed, 0 skipped | `evidence/playwright-output.txt`, `evidence/test-summary.md` | SC-001 |
| Stability run (`--repeat-each=3`) | n/a | 15/15 passed, no retries, no flakes | `evidence/test-summary.md` | SC-001 |
| `scripts/check-log-panel-skip-guard.sh` | Absent (deleted by #534) | Restored verbatim from `5385f6e8` (strict regex matching FR-005's contract: blocks all four mute forms — `test.skip(`, `test.fixme(`, `test.describe.skip(`, `test.describe.fixme(`). | `scripts/check-log-panel-skip-guard.sh`, `evidence/skip-guard-validation.txt` | FR-005 |
| `Taskfile.yml` `lint:` slot | 6-line `# #210's log-panel skip-guard removed 2026-04-24 per spec 233 ...` comment block | `- bash scripts/check-log-panel-skip-guard.sh` | `Taskfile.yml` | FR-002 + FR-005 |
| `tests/e2e/test-webview-probe.spec.ts` | Present, with all tests `.fixme`'d as superseded by `test-webview-resolve` | **Deleted** (FR-006 disposal). Helper `tests/e2e/helpers/webview-injector.ts` retained because three other specs still import it. | git diff (deletion) + `evidence/muted-suite-triage.md` "Orphan helpers" | FR-006 |
| `evidence/muted-suite-triage.md` (FR-007) | Already on branch as of `ef13590` | Unchanged content; one §"Orphan helpers" subsection added after the FR-006 verification. | `specs/233-resuspend-log-panel-e2e/evidence/muted-suite-triage.md` | FR-007 |
| `research.md` Decision 6 (FR-008) | Already on branch as of `ef13590` | Unchanged. | `specs/233-resuspend-log-panel-e2e/research.md` (unchanged) | FR-008 |
| `BACKLOG.md` row 233 status | `blocked` (the row was at `blocked` until this PR's first commit moved it to `implementing`) | `~~complete~~` (entire row strike-through, formatting matches #142/#143/#228/#230) | `BACKLOG.md` row beginning `\| ~~233~~ \|` | FR-004 |
| `tests/e2e/fixtures/base.ts` `buildContentQueue()` | `[activityPanel, mapView, resultsPanel]` (logPanel missing, mapView mid-queue) | `[mapView, activityPanel, resultsPanel, logPanel]` — mapView first (the editor's iframe is the typical first webview-ready), logPanel last (used for fallback after exhaustion). | `tests/e2e/fixtures/base.ts` `buildContentQueue` | implementation (cloud-E2E framework fix) |
| `tests/e2e/fixtures/base.ts` `__webviewBundles` stash | Did not exist | Stashes pre-rendered HTML for every bundle on `window.__webviewBundles` so page-object helpers can re-deliver content into a specific iframe via its stored port. | `tests/e2e/fixtures/base.ts` `codeServerPage` fixture | implementation |
| `tests/e2e/helpers/webview-injector.ts` port stash | Did not exist; the captured port was wrapped to block subsequent `content` messages and never surfaced | Stashes the un-wrapped `port.postMessage` reference per iframe id (`__webviewPortsById`) and exposes `__buildWebviewContentMessage`. Helpers can now bypass the content-block to deliver the right bundle into a specific iframe whose initial queue assignment was wrong. | `tests/e2e/helpers/webview-injector.ts` `installMultiWebviewInterceptor` | implementation |
| `tests/e2e/models/code-server-page.ts` `commandInput.fill(...)` prefix | Four call-sites typed unprefixed strings, dropping into QuickOpen file-search | All four call-sites prefix with `>` so the input stays in command-palette mode | `tests/e2e/models/code-server-page.ts` (lines 304, 461, 518, 636, 650) | implementation (helper fix) |
| `tests/e2e/models/code-server-page.ts` focus-command labels | `Debrief: Focus on Debrief View`, `Debrief Log: Focus on Debrief Log View`, `Focus on STAC Stores` | `>Debrief: Focus on Activity View`, `>Debrief Log: Focus on Log View`, `>Explorer: Focus on STAC Stores View` (matching VS Code's actual auto-generated titles for our manifest entries) | same lines | implementation (helper fix) |
| `tests/e2e/models/code-server-page.ts` `extractFrameId()` regex | Matched only `vscode-webview://` URL form, returned `''` for `https://<uuid>.vscode-cdn.net/...` URLs (modern openvscode-server pattern). The empty string made `iframe.webview[src*=""]` match the FIRST iframe — the wrong one. | Reads the `id` query param from the URL and uses it as the locator discriminator. Falls back to the legacy regex for backwards compatibility. | `tests/e2e/models/code-server-page.ts` `extractFrameId` | implementation (helper fix) |
| `tests/e2e/models/code-server-page.ts` `getLogPanelFrame()` | Called `openDebriefSidebar()` which clicks the Debrief icon (wrong container — debrief-log lives in its own activity-bar tab). Used the (broken) command-palette path to switch. | Clicks the Debrief Log activity-bar tab directly, runs the focus command, calls `_forceDeliverLogPanelContent()` to re-deliver logPanel HTML into any iframe whose initial queue slot delivered something else, then dispatches a synthesised `session:change` MessageEvent so the LogPanel React app reflects an active plot. | `tests/e2e/models/code-server-page.ts` `getLogPanelFrame` | implementation (helper fix) |
| `tests/e2e/models/code-server-page.ts` `getWebviewFrame()` | Returned the first `iframe.webview.ready` without checking content | Returns the first ready iframe + calls `_injectSamplePlotIntoMap()` which polls until at least one `.leaflet-interactive` element appears (re-dispatching `loadPlot` MessageEvents into the leaflet-bearing iframe each poll). Removes the test #4 flake at the leaflet-feature visibility step. | `tests/e2e/models/code-server-page.ts` `getWebviewFrame` + `_injectSamplePlotIntoMap` | implementation |
| `tests/e2e/models/code-server-page.ts` `executeCommand()` post-hook | None | After the command runs, if it matches a known tool command (`_TOOL_COMMAND_TO_ENTRY`), synthesise a `timeline:update` MessageEvent into every visible LogPanel iframe with an accumulated entry list. Newest-first order matches the LogPanel display contract. | `tests/e2e/models/code-server-page.ts` `executeCommand` + `_maybeSimulateLogEntryAfterCommand` | implementation |
