# Generated by `pnpm sync-data` — re-run that script after `pnpm install` to
# repopulate. Keeping these out of git keeps the repo lean and makes it
# obvious that the catalog/fixtures live elsewhere.
data/
playwright-report/
test-results/
node_modules/
playwright/.chromium-path

# #190 — Live LLM transport: operator-supplied runtime config + credentials.
# Both files live at the app root (NOT data/) so they survive sync-data.
/live-config.json
/.env
