{
  "name": "@debrief/nl-demo",
  "version": "0.1.0",
  "description": "Stakeholder demo: NL → CQL2 catalog search (no-build-step React app)",
  "private": true,
  "type": "module",
  "scripts": {
    "sync-data": "node scripts/sync-data.mjs",
    "serve": "pnpm dlx serve@14 --no-clipboard --listen 8080 .",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "node run-playwright.mjs",
    "lint": "echo 'no lint configured for nl-demo (Babel-standalone runtime JSX)'",
    "typecheck": "echo 'no typecheck configured for nl-demo (Babel-standalone runtime JSX)'"
  },
  "dependencies": {
    "@debrief/components": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.0",
    "@sparticuz/chromium": "^143.0.4",
    "esbuild": "^0.24.0",
    "vitest": "^1.0.0"
  }
}
