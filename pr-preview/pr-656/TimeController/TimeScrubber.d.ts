import { TimeScrubberProps } from './types';

/**
 * A draggable time scrubber for navigating through a time range.
 *
 * @example
 * ```tsx
 * <TimeScrubber
 *   timeExtent={[startTime, endTime]}
 *   currentTime={currentTime}
 *   onTimeChange={setCurrentTime}
 * />
 * ```
 */
export declare function TimeScrubber({ timeExtent, currentTime, onTimeChange, disabled, className, }: TimeScrubberProps): import("react/jsx-runtime").JSX.Element;
//# sourceMappingURL=TimeScrubber.d.ts.map