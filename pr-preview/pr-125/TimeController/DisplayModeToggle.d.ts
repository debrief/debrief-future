import { DisplayModeToggleProps } from './types';

/**
 * Toggle switch for track display mode (Full vs Trail).
 * - Full: Shows entire track regardless of time position
 * - Trail: Shows track history from start up to current time position
 *
 * @example
 * ```tsx
 * <DisplayModeToggle
 *   mode={displayMode}
 *   onModeChange={setDisplayMode}
 * />
 * ```
 */
export declare function DisplayModeToggle({ mode, onModeChange, disabled, className, }: DisplayModeToggleProps): import("react/jsx-runtime").JSX.Element;
//# sourceMappingURL=DisplayModeToggle.d.ts.map