// Redeclaration fixture #6 — exported enum shape.
// `PointShape` is a canonical `@debrief/utils` export (a string-union type
// there, but declaring an enum with the same name is still a drift).
export enum PointShape {
  circle = 'circle',
  square = 'square',
}
