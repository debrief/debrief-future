// Redeclaration fixture #7 — default-export function with a collision name.
// `calculateBounds` is a canonical `@debrief/utils` value export.
export default function calculateBounds(): number[] {
  return [0, 0, 0, 0];
}
