/**
 * StacBrowser — top-level orchestrator for three-view synchronization (#132).
 *
 * Composes FilterBar, ExerciseListView, MapView, and TimelineView
 * with synchronized filter state via useBrowserFilter.
 *
 * Placeholder — full implementation in Phase 3 (T051–T058).
 */

import type { StacBrowserProps } from './types';

/**
 * StacBrowser component — placeholder for Phase 3 implementation.
 */
export function StacBrowser(_props: StacBrowserProps): JSX.Element {
  return <div className="stac-browser">StacBrowser placeholder</div>;
}
