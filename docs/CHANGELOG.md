# Changelog

## [Unreleased]

### Added
- **Storyboard — overlap warning for time-range Scenes** (#271) — A passive, non-blocking warning on Storyboard panel rows when two or more time-range Scenes have overlapping `[start, end]` windows in one Storyboard; each offending row names its conflicting partner and can be dismissed (session-scoped, never persisted). Detection is one pure shared helper (`detectSceneOverlaps`) consumed verbatim by both the VS Code extension and the web-shell; the warning reuses the `StaleBadge` per-row slot via a new `OverlapBadge`. Strict interior overlap (touching endpoints are a normal handoff, not a warning); instant Scenes excluded. No schema change, no Python, no service call. Closes the deferred FR-SCO-003 from #263. ([#654](https://github.com/debrief/debrief-future/pull/654))
  - Tests: 28 feature tests passing (16 detection-helper unit, 4 `OverlapBadge` component, 4 VS Code host, 4 Storybook E2E); full component suite 2318 passing, no regressions; 3/3 theme variants.
  - Evidence: `specs/271-scene-overlap-warning/evidence/{test-summary,usage-example,e2e-summary,opening-context}.md` + 4 screenshots (light/dark/vscode + after-dismiss).
- **Properties Panel — feature & sub-feature editing** (#192) — Extends the #447 plot-level Properties Panel into four modes (plot / feature / sub-feature / multi-select summary) through a single `PropertiesPanelDispatch` wrapper that leaves the existing `PropertiesForm` plot path untouched (49/49 #447 tests pass unchanged). Adds one LinkML class `VertexMetadata` and one slot `vertex_metadata: VertexMetadata[]` on `BaseFeatureProperties`, inherited by all 13 concrete feature classes; the address is a structured `path` string (`positions/N` / `rings/R/vertices/V` / `vertices/V` / `vertex/0`) per the #053 selection-path convention. Closes the Article I.3 silent-save gap from `/speckit.review` via an integrated `saveSession-integration.test.ts` that asserts the four-way invariant (writer / provenance / clearAll / dirty) plus the read-only escalation. Ships all 7 user stories: edit a feature (US-1), annotate a track point (US-2), mode swap preserves staged edits (US-3), modifier-aware multi-feature selection on map + Layers (US-4), pre-flight + post-write read-only detection (US-5), one-click revert of per-platform overrides (US-6), and vertex annotation across Polygon / LineString / MultiPoint / Point (US-7). One breaking change: `MapView.onSelect` payload shape — same prop name, updated at all three consumers. Zero new runtime dependencies; one new package subpath `@debrief/session-state/browser` to keep the VS Code webview's iife bundle free of Node-only Express/parseurl code. ([#641](https://github.com/debrief/debrief-future/pull/641))
  - Tests: **4 670 passing** across pytest (916, +53 new), components vitest (2 250, ~+300 new), session-state vitest (675, +14 new), vscode vitest (780), and 49 web-shell + Storybook Playwright cases. Zero regressions.
  - Evidence: `specs/192-properties-panel-feature-edit/evidence/{test-summary,usage-example,round-trip-evidence,webview-e2e-summary,opening-context}.md` + 14 screenshots under `screenshots/` (7 Storybook + 7 web-shell workflow frames).
  - Known follow-ups: add `@debrief/data/browser` subpath to retire the inline `PLATFORM_REGISTRY_MIRROR` in `FeatureEditorMode.tsx`; surface a Save action in the panel header; convert workflow PNG sequences to GIFs once `ffmpeg` is on the CI image.
- **Backlog Navigator — lazy-load mobile component tree** (#247) — Code-splits `apps/backlog-navigator/src/components/mobile/*` (CardList, ItemCard, BottomSheet, BottomSheetEditor, DescriptionEditorScreen, StickyPushBar, MobileFilterBar) behind two `React.lazy` boundaries (App.tsx + EditorOverlayProvider.tsx) so a desktop visitor never downloads modules they will never render. A new `MobileSkeleton` Suspense fallback covers the cold-mobile-load gap; a new `ChunkErrorBoundary` catches `ChunkLoadError` / Vite's `Failed to fetch dynamically imported module` / Safari's `Importing a module script failed` and offers a Reload action. Re-contracts `scripts/check-bundle-size.mjs` to identify the desktop entry chunk via Vite's build manifest (`build.manifest = true`) and gzip-measure that file alone — without this change the split would mechanically not reduce the measured number. Re-baselines `scripts/bundle-baseline-244.json` to the post-split entry-chunk-only floor: 126,009 B gz (down from 132,742 B gz pre-split on the same git tree — a 23.84 KB raw / 6.73 KB gz drop, satisfying SC-001). Zero new runtime or dev dependencies. ([#601](https://github.com/debrief/debrief-future/pull/601))
  - Tests: 139/139 navigator Vitest (incl. 14 new), 7/7 bundle-guard `node --test`, 6/6 effective Playwright E2E across mobile-iphone / tablet-portrait / tablet-landscape projects.
  - Evidence: `specs/247-lazy-mobile-bundle/evidence/{test-summary,usage-example,bundle-measurements,opening-context,screenshots/cold-mobile-skeleton-mobile-iphone.png,screenshots/cold-mobile-skeleton-tablet-portrait.png,screenshots/recovery-banner-mobile-iphone.png,screenshots/recovery-banner-tablet-portrait.png}`.
  - Follow-ups: #252 (symmetric desktop split), #253 (ADR for entry-chunk-only guard), #254 (reusable `PanelErrorBoundary`).
- **VS Code E2E webview reliability — research sprint** (#142) — Identifies and fixes the root cause behind ~15 self-skipping E2E test files. `WebviewViewPane.oc()` in openvscode-server v1.109.5 gated webview creation on `isBodyVisible()`, which returns `false` in headless — so `resolveWebviewView()` was never called and the extension's React/Leaflet content never rendered in `#active-frame`. **Patch 3** removes the visibility gate so `pc()` (webview creation) is always called while preserving claim/release semantics; `pc()`'s internal `if(this.g)return;` keeps creation idempotent. Five previously-skipped spec files unskipped (`test-load-display`, `test-catalog-browse`, `test-selection-sync`, `test-time-controller`, `test-error-feedback`); remaining missing-feature tests converted to `test.fixme()` with backlog refs. All four `patch-webview.sh` patches now version-guarded (exit 1 on missing pattern) and idempotent. New `tests/e2e/test-webview-resolve.spec.ts` validates webview disposal/re-creation on sidebar toggle. CI workflow `e2e.yml` adds a `patch-webview.sh` smoke check; web-shell + VS Code E2E suites run as parallel jobs within the 25-minute budget. Subsumes #135. ([#548](https://github.com/debrief/debrief-future/pull/548))
  - Tests: 2/2 validation tests passing (sidebar composite reveal 5.7 s, dispose/re-create 8.7 s); 5 spec files unskipped (SC-004 ✓); web-shell E2E suite (81 tests) regression-free.
  - Evidence: `specs/142-vscode-e2e-webview-reliability/evidence/{test-summary,usage-example,root-cause-analysis,config-sample,validation-output,opening-context,screenshots/sidebar-webview-resolved.png}`.
  - Unblocks: #233 (re-activate Log Panel E2E suite).
- **Storyboard edit suite — webview wiring + web-shell harness + error triage** (#230 / spec 230) — Closes the VS Code webview wiring gap from #218. Every Scene row gains a chevron + double-click + right-click overflow menu with six in-panel actions (Edit, Update-to-current, Duplicate, Copy-to-other, Delete, Refresh-thumbnail); single-form-at-a-time invariant. New `useStoryboardEditReducer` hook is the shared behavioural layer consumed by the VS Code webview AND the new web-shell harness (`?storyboard-edit-harness=1`) — one reducer, zero drift between surfaces. Query-string knobs (`?stale=`, `?pendingDelete=`, `?missingData=`) let Playwright set up deterministic scenarios without VS Code. `storyboardPanelView.refresh()` + `applySnapshot()` compose the 3 new edit view-models while preserving the O(active-storyboard Scenes) invariant (FR-008 / review 13A from #218). Triages two pre-existing errors: `MapView` now emits bounds on mount + `map.whenReady` (fixes first-capture "viewport not reported"); `stacService.loadPlot` writes four distinct diagnostic strings at every null-return branch (root cause of `Failed to load plot` now attributable from the output channel). ([#534](https://github.com/debrief/debrief-future/pull/534))
  - Tests: 88 new (26 reducer + 22 component + 7 Playwright + 7 query-string + 4 panel + 2 stac-diagnostic + 20 refreshed view-provider); 2,406 / 2,410 pass with 4 pre-existing skips. Zero regressions in the #218 baseline.
  - Evidence: 6 screenshots (panel-default, edit-form-open, overflow-menu, undo-toast, stale-badge, missing-data) captured under `specs/218-storyboarding-edit/evidence/screenshots/` per FR-040. `specs/230-storyboard-edit-wiring/evidence/{test-summary,usage-example,webview-e2e-summary,viewport-diagnostic-log,stac-load-diagnostic-log,baseline-verify,opening-context}`.
  - Zero new runtime dependencies (Article IX preserved) — reducer is `useReducer`; SceneOverflowMenu is native `<menu role="menu">`.
  - Follow-up spec 232 captures the five intentionally-deferred polish items (interactive Storybook stories, code-server E2E, axe audit, perf budget, remaining Playwright scenarios + GIF).
- **Regenerate blog archive from specs** (#228) — One-shot Python script walks every `specs/NNN-<slug>/` directory, classifies each into `unified` / `epic-member` / `composite-member` / `skipped`, and emits 73 new post files (56 unified, 3 epic rollups for E02/E05/E08, 14 composites) plus `ARCHIVE-REBUILD.md` at repo root with index + four-step runbook for the `debrief.github.io` maintainer. Charter-first epic detection via BACKLOG.md Epics table (research R1); composite clustering via ≤5-day proximity + ≥1 non-noise shared tag (FR-003); 6–10 day near-misses surfaced as Unresolved Groupings for manual promotion. Zero existing files under `specs/*/` modified — structural no-overwrite guarantee via `AtomicWriter` stage-and-promote with tempdir rollback. Script + 54 tests deleted in the same PR per FR-009; preserved in commit history `19406178..6fc7cb17` for reviewer inspection (NFR-004). ([#518](https://github.com/debrief/debrief-future/pull/518))
  - Tests: 54/54 passing, 77% coverage, 11/11 C1–C11 contract tests green.
  - Real run: 155 specs scanned, 129 eligible, 73 posts generated, 43 unresolved groupings, 0.3 s runtime.
  - Evidence: `specs/228-regenerate-blog-archive/evidence/{test-summary,usage-example,cli-demo,help-output,dry-run-index,run-log,no-overwrite-proof,corpus-coverage,opening-context}`.
  - Zero new runtime dependencies (PyYAML already vendored via LinkML transitively; `gh` optional with graceful fallback).
- **Storyboarding: Panel + Playback** (#217 / E024 3/4) — Multi-Storyboard dropdown, TransportRow (Prev/Next + scoped Left/Right-arrow), Leaflet `flyTo` animation, scrub-window lock via `TimeRangeViewProvider.setScrubbableRange`, on-map `SceneRectangleLayer` overlay, missing-data `HardBlockModal` with `Jump Past` chain-walker. `StoryboardPlaybackService` on the extension host with three-trigger (`moveend` / `onDidChangeVisibility(false)` / `durationMs + 250ms` safety timer) transition-clear invariant — idempotent by token. `getMostRecentlyModifiedStoryboard` query added to #215 CRUD. Zero new runtime dependencies. ([#505](https://github.com/debrief/debrief-future/pull/505))
  - Tests: ~154 new unit tests across 11 suites (85 VS Code extension, ~69 `@debrief/components`); every acceptance scenario in `spec.md` maps 1:1 to a named test.
  - Totals at `a0c6bd74`: 1812 Python passed; 1725 components; 622 session-state; 458 VS Code extension. 28 pre-existing failures ledgered (Windows path-separator + pre-existing config-store), none introduced by #217.
  - Evidence: `specs/217-storyboarding-playback/evidence/test-summary.md`, `usage-example.md`, `feature-integration.md`, `screenshots/README.md`.
  - Deferred per Blocker #143: 9 Storybook theme PNGs, interaction GIF, 2 E2E PNGs (same constraint as every VS Code webview E2E in this repo; unblock plan in screenshots README).
- **[E11] Audit non-LinkML type declarations** (#206) — enumerates every named TypeScript `interface` / `type` / `enum` across `apps/`, `shared/`, and `services/` (885 declarations, 317 files, 25 drift clusters) and classifies each into one of five E11 buckets. Primary artefact: [docs/type-audit-2026.md](type-audit-2026.md). Commits a deterministic scanner + generator under `scripts/audits/type-audit/` for future re-runs. Opens 6 new backlog items (#222 MCP transport, #223 STAC hand-types, #224 session-state wire, #225 loader↔main IPC, #226 residual drift, #227 Storybook/Props convention rollup) and re-uses #204 for GeoJSON hand-types; every actionable row (134/134) resolves to a backlog ID. ([#504](https://github.com/debrief/debrief-future/pull/504))
  - Tests: 18/18 scanner unit tests passing (vitest); full-repo scan runs in ~4 s and validates against the committed JSON-Schema contracts (ajv 2020-12).
  - Evidence: `specs/206-audit-non-linkml-types/evidence/{test-summary,usage-example,scanner-run,scan-output.sample,ajv-validation,backlog-diff,report-link,rerun-methodology}`.
  - Zero production source changes — matches spec SC-005. Root devDeps added: `typescript`, `vitest`, `ajv`, `@types/node`.
- **Unify `shared/components` bounds utilities with `@debrief/utils`** (#213 / spec 219) — Deletes the 215-line duplicate `shared/components/src/utils/bounds.ts` and consolidates all nine bounds helpers into `@debrief/utils`. Ports `expandBounds`, `isPointInBounds`, `bboxOverlapsViewport`, `viewportToBounds`, `filterBySpatialExtent` from `shared/components`. Adds pre-computed `feature.bbox` fast-path to `calculateBounds` (absorbs #211) — O(n features) map-fit latency for STAC collections. `calculateBounds` accepts `DebriefFeature[]`, `SafeFeature[]`, `GeoJSONFeature[]`, or a `FeatureCollection`-shaped object without casts (Article XV, FR-001). `shared/components` barrel re-exports unchanged — zero external consumer churn. ([#497](https://github.com/debrief/debrief-future/pull/497))
  - Tests: 301/301 `@debrief/utils`, 1647/1647 `@debrief/components`; `bounds.test.ts` grew from ~26 to 76 assertions; 6 compile-time type assertions in `bounds.types.test-d.ts`.
  - Behavioural change: malformed `feature.bbox` (non-finite values or length < 4) now falls back to the coordinate walk silently. The previous `shared/components` implementation used `?? 0` padding on missing bbox entries, which could produce silently incorrect bounds — this is a bug-fix (research.md R-002).
  - Evidence: `specs/219-unify-bounds-utilities/evidence/test-summary.md`, `usage-example.md`, `before-after.md`, `consolidation-metrics.md`, `dep-graph-check.md`.
  - Closes: #211 (bbox fast-path absorbed). Follow-ups: #212 (LinkML-generated SafeFeature/GeoJSONFeature).
- **Consolidate spatial types in LinkML + lat/lon ↔ GeoJSON converters** (#203) — Retires the three-way duplication of `Coordinate`, `ViewportPolygon`, and `TimeFilter` across `shared/components`, `services/session-state`, and `@debrief/schemas`. The LinkML schema is now the only place these types are defined; hand-authored TS duplicates are deleted. Adds two pure converter helpers to `@debrief/utils` — `toGeoJSONCoord` / `fromGeoJSONCoord` — that confine tuple-form handling to a narrow GeoJSON/Leaflet boundary (RFC 7946 axis order). Validators (`validateCoordinate`, `validateViewportPolygon`, `calculateViewportCenter`) relocate to `@debrief/utils` so components and session-state share one implementation. Introduces `coerceViewport` in `services/session-state/src/persistence/load.ts` for silent legacy-tuple rehydration; bumps `SCHEMA_VERSION` 1.0.0 → 1.1.0; removes the blind `setViewport(... as never)` cast at `load.ts:125`. ([#490](https://github.com/debrief/debrief-future/pull/490))
  - 32 new TDD unit tests (converters, validators, coerceViewport); integration test loads a v1.0.0 tuple-form payload end-to-end.
  - Tests: 2 660 / 2 664 passing across primary suites (utils 202, session-state 617, components 1 601, schemas Python 684, schemas TS 11); `pnpm -r typecheck` clean.
  - Evidence: `specs/203-spatial-types-linkml/evidence/test-summary.md`, `usage-example.md`, `schema-diff.md`, `round-trip-evidence.md`, `call-site-audit.md`.
  - Follow-ups: Python converter helpers, ESLint rule for tuple-construction, delete `coerceViewport` legacy branch after migration window, remove `TimeFilter` from `services/session-state-py` public API.
- **Consolidate Bounds Utilities into @debrief/utils** (#200) — Retires the 95%-identical duplicate `calculateBounds`/`mergeBounds` copy at `apps/vscode/src/utils/bounds.ts` (-116 LOC) and its duplicate test (-166 LOC); the monorepo now carries exactly one implementation under `shared/utils/` for the generic-GeoJSON call-site family. Widens the canonical parameter to a structural minimum so `SafeFeature[]`, `GeoJSONFeature[]`, and `DebriefFeature[]` flow through without `as`-casts at the call site; introduces a single named narrowing gate (`coerceCoordinates`) anchored in source to constitution Article XV.5. Also fixes the adjacent silent-miss bug `/speckit.review` surfaced: `fitToSelection()`'s 35-line inline loop silently skipped Polygon / MultiPolygon / MultiPoint / MultiLineString selections; now a 3-line delegation to the consolidated utility honours every supported geometry type. ([#482](https://github.com/debrief/debrief-future/pull/482))
  - 15 new bounds assertions (5 narrowing-gate shape-mismatch, 4 null-geometry regression, 6 per-geometry-type correctness) — SC-006, SC-007, SC-009
  - Tests: 176/176 passing in `@debrief/utils`; 2 264 passing total in this feature's scope; 0 new errors in `pnpm -r typecheck` and `uv run pyright`
  - Evidence: `specs/200-bounds-consolidation/evidence/test-summary.md`, `usage-example.md`, `canonical-grep.txt`, `narrowing-gate-source.md`, `before-after-fittoselection.md`, `geometry-type-matrix.md`, `selection-zoom-smoke.md`, `plot-open-smoke.md`, `typecheck-output.txt`, `preflight-grep.txt`
  - Follow-ups: #211 (bbox fast-path), #212 (LinkML `SafeFeature`/`GeoJSONFeature`), #213 (`shared/components` bounds unification), #214 (drift-prevention rule — keeps SC-001 durable)
- **Knip Config for Electron Loader + CI Gate** (#202) — New root `knip.json` declaring the Electron loader's three entry points (main, preload, renderer); deletes `apps/loader/src/main/updater.ts` (a genuine orphan); pins `knip@^5` as a dev dependency; adds `task knip` target and `Run knip` step in CI between lint and typecheck. Loader main-tree unused-file findings: 10 → 0. ([#474](https://github.com/debrief/debrief-future/pull/474))
  - Gate is specific to `apps/loader/src/main/` paths via `jq` on knip's JSON reporter; pre-existing findings elsewhere are visible but non-blocking
  - Contract schema at `specs/201-knip-loader-config/contracts/knip-config.schema.json` rejects `ignore` keys to prevent future silencing
  - Tests: 11/11 checks passing (contract validation, pre/post knip diff, stress test for regression detection)
  - Evidence: `specs/201-knip-loader-config/evidence/verification-record.md`, `test-summary.md`, `usage-example.md`, `ci-run-transcript.md`, `knip-config-sample.md`, `validation-output.txt`
- **Regenerate Sample Catalog** (#184) — New `scripts/regenerate-sample-catalog.py` orchestration script that nukes and rebuilds `preview/workspace/samples/local-store/` via the enriched import pipeline. 73 items regenerated, all with `debrief:platforms` structured arrays; zero deprecated flat aggregate fields remain. Extends `enrich-legacy-catalog.py` with `derive_vessel_fields()` to populate `vessel_type`/`vessel_role`/`domain` from the `vessel_class` path (FR-006). [E10 Phase 3] ([#427](https://github.com/debrief/debrief-future/pull/427))
  - Pipeline: extract → stage → delete → reimport → enrich (safe-by-construction, `--stage-only` dry-run supported)
  - Idempotent: two consecutive runs produce identical 73 items / 500 warnings (`random.Random(42)`)
  - Tests: 1,643 Python passed, all TypeScript passed, 79 Playwright E2E passed
  - Evidence: `specs/184-regenerate-sample-catalog/evidence/test-summary.md`, `usage-example.md`, `validation-output.txt`, `item-before.json`, `item-after.json`
- **Import Platform Warnings** (#182) — Post-parse validation checks extracted `platform_id` values against the platform registry after import; emits advisory `UNREGISTERED_PLATFORM` warnings for unregistered platforms. Import always succeeds regardless of registry coverage. [E10 Phase 2]
  - New function: `_validate_platform_ids()` in `import_catalog.py` with registry loading and graceful fallback
  - Warning codes: `UNREGISTERED_PLATFORM` (per platform per file), `REGISTRY_UNAVAILABLE` (registry load failure)
  - Tests: 17/17 passing (9 unit + 8 integration), 344 existing tests unaffected
  - Evidence: `specs/182-import-platform-warnings/evidence/test-summary.md`, `usage-example.md`, `sample-warnings.json`
- **LinkML Per-Platform Override Fields** (#181) — Six optional override fields on TrackProperties, new PlatformRecord entity, and `debrief:platforms` structured array replacing flat aggregates on STAC extension. Full consumer code migration across filter engine, VS Code, web-shell, and Python services. [E10 Phase 1]
  - Schema: 3 LinkML YAML files modified, VesselDomainEnum moved to common.yaml for cross-module use
  - New entity: PlatformRecord (id required + 6 optional classification fields)
  - Breaking: removed vessel_classes, nationalities, track_names from StacExtensionProperties/StacItemSummary
  - Migration: ~40 files updated across TypeScript (types, services, filter engine, stories, tests) and Python (STAC service, collection summaries)
  - Fixtures: 100 exercise fixtures regenerated with debrief:platforms format, 7 new golden fixtures
  - Tests: 2921/2921 passing, 0 failures
  - Evidence: `specs/181-linkml-platform-overrides/evidence/test-summary.md`, `usage-example.md`, `round-trip-evidence.md`
- **Platform Registry** (#180) — Shared JSON registry defining a vessel class taxonomy tree with 10 seeded platforms. Dual Python (`debrief-data`) and TypeScript (`@debrief/data`) loaders with resolve, enumerate, tree traversal, and class validation APIs. [E10 Phase 0]
  - New package: `shared/data/` with `platform-registry.json`, Python `registry.py`, TypeScript `registry.ts`
  - Golden fixture cross-language parity: both loaders produce identical results
  - Load-time validation: duplicate IDs, missing fields, malformed JSON
  - Tests: 66/66 passing (33 Python + 33 TypeScript), zero new dependencies
  - Evidence: `specs/180-platform-registry/evidence/test-summary.md`, `usage-example.md`, `validation-output.txt`
- **Sensor Rendering** (#118) — Leaflet custom canvas layer for sensor bearing lines, ambiguous bearings, sensor arcs, snail mode time-trail fading, contact labels, and line styling. [E07 Phase 3]
  - New components: `SensorBearingLayer.tsx` (canvas layer), `sensor-utils.ts` (geometry/interpolation/colour utilities)
  - 7 Storybook stories covering all rendering modes
  - Tests: 81/81 passing (67 unit + 14 component), no new dependencies
  - Evidence: `specs/118-sensor-rendering/evidence/test-summary.md`, `usage-example.md`
- **REP Sensor Import** — Parse `;SENSOR:` (v1), `;SENSOR2:` (v2), `;SENSOR3:` (v3), and `;SENSORARC` lines from REP files, embedding sensor contacts into `TrackFeature.properties.sensors[]` via `pending_sensor_data`. Replaces standalone annotation features.
  - New module: `sensor_parser.py` with 4 parser functions + contact grouping
  - Tests: 90/90 passing (56 new), 10k-line performance benchmark under 1s
  - Evidence: test-summary.md, usage-example.md, sample-input.rep, parsed-output.json
- **Tabular Results Panel** (#177) — Display `debrief-calc` tool outputs as tables (flat statistics) or charts (Vega-Lite time-series) in a panel beneath the map. Supports Save / Save As to CSV in the plot's assets folder, surfaces saved files in the LayersToolbar Associated Files dropdown, and wires Open / Reveal in Explorer / Open With file actions in the web-shell.
  - New components: `TableRenderer`, expanded `ChartPanelWrapper` with save UI and tab management
  - New utilities: `buildCsvContent`, `generateCsvFilename`, `sanitizeFilename`, `formatCsvValue` in `@debrief/utils`
  - Auto-synthesis of table datasets from MCP tools that return `properties.statistics` (track-stats, area-summary)
  - All user-facing strings externalised via `ResultsPanelLabels` interface (Constitution Article XI)
  - Tests: 33 new unit tests (26 CSV + 7 TableRenderer), 8 new E2E tests across save flow, file actions, and panel persistence
  - Evidence: `specs/177-tabular-results-panel/evidence/test-summary.md`, `usage-example.md`
- **VS Code integration SRD** for the Tabular Results Panel — captures the deferred work to bring feature parity to the VS Code extension (`docs/tabular-results-vscode-integration-srd.md`)

### Fixed
- **GoldenLayout panel persistence** — sidebar panels (Navigation, Activity, Log) no longer disappear after navigating between plots. Layout version bumped from 1 to 2; corrupted layouts that pass type validation but lack essential panels are now rejected and replaced with the default. Layout saves are suppressed during reset to prevent intermediate empty state from being persisted.

## [2026-03-18]

### Added
- **REP Loader Temporal Metadata** — Compute `start_datetime`/`end_datetime` from track position timestamps during REP file loading; enables accurate Timeline/Gantt view and Duration filter.
  - Tests: 9/9 passing (9 new)
  - Evidence: test-summary.md, usage-example.md, sample-request.json, sample-response.json

## [2026-03-07]

### Added
- **Vessel Taxonomy and Hierarchical Filtering** — Human-readable labels, in-menu type-ahead search, per-node match counts, and current-selection marking for the vessel class filter dropdown.
  - Tests: 944/944 passing (72 new)
- **Saved Filter Configurations** — Save/load/delete named filter sets as CQL2 JSON; historic filters dropdown for the STAC Browser filter bar.
  - Tests: 120/120 passing (47 new)
  - Evidence: test-summary.md, usage-example.md

## [2026-03-06]

### Added
- **Filter Bar with Lozenge UI and AND/OR Logic** — Persistent filter bar with pill-shaped lozenges, all 10 SRD filter types, OR groups with drag-to-group, and CQL2 serialisation.
  - Tests: 64/64 passing
  - Evidence: test-summary.md, usage-example.md
- **Client-Side CQL2 Filter Engine** — Reference implementation of CQL2 AND/OR filter logic for 9 metadata types, operating on mock STAC items; validates query model without backend.
  - Tests: 74/74 passing
  - Evidence: test-summary.md, usage-example.md, filter-output-samples.json
- **STAC Extension Spec + Mock Data Fixtures** — Define `debrief:` STAC extension namespace with 6 properties; generate 100 deterministic fixture items for Discovery UI development.
  - Tests: 210/210 passing
  - Evidence: test-summary.md, usage-example.md, round-trip-evidence.md, validation-output.txt
- **End-to-End Workflow Tests** — Dual-platform E2E test suite: 18 VS Code E2E specs + 13 web-shell specs with real Python services. ([#300](https://github.com/debrief/debrief-future/pull/300))
  - Tests: ~25 active, ~28 fixme (features pending implementation)
  - Evidence: test-summary.md, usage-example.md, integration-flow.md, 4 screenshots
