// Fixture: file imports from @debrief/schemas — the declaration here
// should auto-tag as schema-rooted-candidate.
import type { Feature } from '@debrief/schemas';

export type TrackFeature = Feature;
