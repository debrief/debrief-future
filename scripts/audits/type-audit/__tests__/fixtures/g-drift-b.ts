// Fixture: second half of a drift pair — same declaration name,
// different shape. Drift cluster must emit both memberIds.
export interface Platform {
  name: string;
  category: string;
  hullNumber: number;
}
